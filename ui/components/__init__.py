"""Reusable Streamlit widgets shared across pages.

Populated by later M4 bullets — e.g. the config-builder sidebar and the KPI
summary card. Kept separate from ``ui/pages/`` so a widget used on more than one
page is defined once.
"""
